Java Simple HTTP Server ver 1.1:
by Truong Van Quang

v1.1.0:

*BUG FIX*

+ RESOURCES LEAK BUG caused by unclosed server thread is now fixed.

+ CLOSING TERMINAL WINDOW no longer creates lag.

v1.1.1:

*BUG FIX*

+ RESOURCES LEAK BUG after clicking "STOP" is now fixed.

*PROBLEM*:

+ Technically no CPU and MEMORY optimization.

+ Program structure is garbage. Very coupled Code.